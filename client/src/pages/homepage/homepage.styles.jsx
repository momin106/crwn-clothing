/* Since we are not using these .scss files, its just here for the better picture what we are translating into jsx*/
import styled from 'styled-components';

export const HomePageContainer = styled.div`
	display: flex;
	flex-direction: column;
	align-items: center;
`;
